const {
  NODE_ENV,
  PORT,
  POSTGRES_DATABASE_URL,
  MONGO_DATABASE_URL,
  JWT_SECRET,
} = process.env;

export default {
  NODE_ENV,
  PORT,
  POSTGRES_DATABASE_URL,
  MONGO_DATABASE_URL,
  JWT_SECRET,
};